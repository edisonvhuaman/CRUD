CREATE DATABASE CRUD1;
USE CRUD1;
CREATE TABLE PERSONA (
IDPERSONA INT AUTO_INCREMENT PRIMARY KEY,
    NOMBRE VARCHAR(20),
    EDAD INT,
    USUARIO VARCHAR(10),
    CONTRASENA VARCHAR(10)
);

#insertando primer dato
USE CRUD1;
INSERT INTO persona (IDPERSONA, NOMBRE, EDAD, USUARIO, CONTRASENA)
VALUES(1, 'EDISON', 21, 'ELMON', '12345');